post_install() {
    echo -e "\n** The files have been installed in /etc/skel/.config/powermenu **"
    echo -e "\n** The files have been installed in /etc/skel/.bin **"
    echo -e "\n** The files have been installed in /usr/local/bin **\n"
}

post_upgrade() {
    post_install
}
